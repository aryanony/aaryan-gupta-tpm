{
  "name": "aaryan-gupta-tpm",
  "version": "1.2.0",
  "scripts": {
    "build": "node build.js",
    "start": "npx serve dist",
    "dev": "node build.js && npx serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0"
  }
}
